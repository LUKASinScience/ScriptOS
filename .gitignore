# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Build artifacts (PyInstaller / DMG)
build/
dist/
ScriptOS.spec
ScriptOS.dmg

# Test run output
tests/fixtures/runs/
runs/

# zensical docs site build output + cache (regenerate with: zensical build)
site/
.cache/

# pytest cache
.pytest_cache/

# Editors
.vscode/
.idea/
.DS_Store

# Claude Code local session state + historical planning docs — local-only, never shared
.claude/
CLAUDE.md
CLAUDE_shared.md
scientific-tool-wrapper-v2.md

# Local-only asset generators — outputs (app_icon.*, docs/assets/*.png) stay tracked,
# scripts themselves are only ever run locally
build_icon.py
build_mockup.py
